import { AppShell } from "@/components/app-shell";
import { UploadForm } from "@/features/songs/upload-form";

export default function UploadPage() {
  return (
    <AppShell>
      <UploadForm />
    </AppShell>
  );
}
